<div class="row">

<h2><%= @artist_albums['name'] %></h2>

	<div class="table-responsive">

		<table class="table table-hover" summary="Lista de Albums">
			<thead>
			    <tr class="header">
			      <th>Album</th>
			      <th>Artista</th>
			      <th>Año</th>
			      <th>URI</th>
			      <th>Acciones</th>
			    </tr> 
			</thead>
			<tbody>
				<% @artist_albums['albums'].each do |f| %>
					<tr>	
					   <td> <%= truncate(f['album']['name']) %>  </td>
					   <td> <%= f['album']['artist'] %>  </td>
					   <td> <%= f['album']['released'] %>  </td>
					   <td> <%= f['album']['href'] %>  </td>
					   <td class="actions">
				        <%= link_to("Ver Pistas", {:action => 'show_album_tracks', :uri => f['album']['href'] }, :class => 'btn btn-default btn-xs') %>
				       </td>
					</tr>
				<% end %>
			</tbody>
		</table>

	</div>

</div>